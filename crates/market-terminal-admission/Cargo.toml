[package]
name = "market-terminal-admission"
version = "0.1.0"
edition = "2021"
description = "Host-neutral aggregate request admission for Market Terminal hosts"
license = "MIT"
repository = "https://github.com/thedjpetersen/market-terminal"

[dependencies]
market-terminal-application = { path = "../market-terminal-application" }
